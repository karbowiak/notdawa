# Secrets & build output
/.env
/bin/
/dist/

# Top-level scratch data + downloaded extracts (the embedded seed CSVs live under
# internal/ingest/data/ and are NOT matched by this root-anchored rule)
/data/
*.zip
*.gpkg
*.gml

# Local / editor / tooling
.DS_Store
.vscode/
.claude/

# Vendored third-party docs + internal working notes (not part of the implementation)
/docs/
